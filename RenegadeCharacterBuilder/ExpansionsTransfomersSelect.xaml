<Page x:Class="RenegadeCharacterBuilder.ExpansionsTransfomersSelect"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RenegadeCharacterBuilder"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White"
      Title="ExpansionsTransfomersSelect">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel 
            
                    Margin="10,10,10,10"
                    
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock 
                       Text="Select Expansions"
                FontSize="33"
                ></TextBlock>

            <CheckBox Content="Core Rule Book" IsChecked="True"/>  
            <CheckBox Content="Decepticon Directive"/>
            <CheckBox Content="Technorganic Secrets"/>
            <CheckBox Content="Transformers One"/>
            <CheckBox Content="Field Guide to Action and Adventure (Coming soon..)" IsEnabled="False"/>

            


        </StackPanel>
        <Button     
            Grid.Row="3"
            Grid.Column ="2"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right"
            Content="Countinue"
            Margin="10"
            Height=" 80"
            Width="400"/>

    </Grid>
</Page>
