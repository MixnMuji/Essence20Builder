<Page x:Class="RenegadeCharacterBuilder.RolesTF"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RenegadeCharacterBuilder"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RolesTF"
      Background="White">

    <Grid>
        <Grid>

            <TextBlock FontSize="28" Text="{Binding Role}" Margin="10"/>

            <ItemsControl ItemsSource="{Binding Levels}" Margin="10,50,10,10">
                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <Expander Header="{Binding Level}">
                            <TextBlock Text="{Binding Perks}" Margin="10"/>
                        </Expander>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Grid>
    </Grid>
</Page>
