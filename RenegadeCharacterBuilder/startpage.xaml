<Page x:Class="RenegadeCharacterBuilder.startpage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RenegadeCharacterBuilder"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="startpage"
      Background="White">

    
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0"
           FontSize="22"
           Content="Choose a game"/>

            <ComboBox Grid.Row="2" 
              Name="ComboBox1" Width="200" Height="30" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,10,0,0">

            </ComboBox>

            <Button Grid.Row="2"
            Margin="20"
            Content="Proceed"
            Width="300"
            Height="100"
            Click="proceed_Click"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"/>

            <Frame Name="MainFrame"/>
        </Grid>
    
</Page>
